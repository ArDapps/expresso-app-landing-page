import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    unoptimized: true, // For landing page screenshots if they are local
  },
};

export default nextConfig;
